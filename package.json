{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "rpc:build": "frida-compile server/rpc/index.ts -o _rpc.js -c",
    "rpc:watch": "frida-compile server/rpc/index.ts -o _rpc.js -w"
  },
  "dependencies": {
    "frida": "^16.6.6",
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  }
}
